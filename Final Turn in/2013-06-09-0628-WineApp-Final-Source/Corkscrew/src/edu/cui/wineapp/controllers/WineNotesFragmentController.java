package edu.cui.wineapp.controllers;

import android.app.Activity;

public class WineNotesFragmentController {
	/*
	private WineNotesFragmentManager currManager;
	private WineNotesFragmentView currView;
	
	public WineNotesFragmentController(Activity activity, WineNotesFragmentView currView, WineNotesFragmentManager currManager){
		this.currManager = currManager;
		this.currView = currView;
		
		currManager.updateComments();
		initCommentBox();
	}

	private void initCommentBox() {
		currView.getCommentBox().setOnEditorActionListener(currManager.getEAL(currView.getCommentBox()));
		currView.getCommentBox().setOnClickListener(currManager.getOCL(currView.getCommentBox()));
	}
*/
}
